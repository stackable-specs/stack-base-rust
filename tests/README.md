# Tests directory

This directory contains integration tests for the stack-base-rust project.

Unit tests are colocated with source code in `src/` using `#[cfg(test)] mod tests { ... }`.

See `docs/specs/quality/unit-testing.md` for testing conventions.